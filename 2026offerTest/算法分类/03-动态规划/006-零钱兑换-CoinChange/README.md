# 006. 零钱兑换 (Coin Change)

**难度**: 中等  
**标签**: 动态规划, 背包问题

---

## 📝 题目描述

给你一个整数数组 `coins`，表示不同面额的硬币；以及一个整数 `amount`，表示总金额。

计算并返回可以凑成总金额所需的 **最少的硬币个数**。如果没有任何一种硬币组合能组成总金额，返回 `-1`。

你可以认为每种硬币的数量是无限的。

---

## 💡 示例

### 示例 1
```
输入: coins = [1, 2, 5], amount = 11
输出: 3
解释: 11 = 5 + 5 + 1
```

### 示例 2
```
输入: coins = [2], amount = 3
输出: -1
```

### 示例 3
```
输入: coins = [1], amount = 0
输出: 0
```

---

## 🎯 解题思路

**核心思想**: 完全背包问题 - 动态规划

**状态定义**: 
- `dp[i]` 表示凑成金额 `i` 所需的最少硬币数

**状态转移方程**: 
- `dp[i] = min(dp[i], dp[i - coin] + 1)` 对于每个 `coin`

**步骤**:
1. 初始化 `dp` 数组为 `amount + 1`（一个不可能达到的最大值）
2. `dp[0] = 0`（基础情况）
3. 遍历每个金额 `i`，尝试使用每种硬币
4. 返回 `dp[amount]`，如果仍为初始值则返回 -1

---

## 📊 复杂度分析

- **时间复杂度**: O(amount × coins.length) - 两层循环
- **空间复杂度**: O(amount) - dp 数组

---

## 🔑 关键点

- 这是典型的完全背包问题（物品可以无限使用）
- 初始化时用 `amount + 1` 而非 `int.MaxValue`，避免溢出
- 外层遍历金额，内层遍历硬币（顺序可交换）

---

## 📚 相关题目

- 零钱兑换 II（求组合数）
- 完全平方数
- 组合总和 IV
