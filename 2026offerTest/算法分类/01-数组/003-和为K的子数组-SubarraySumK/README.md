# 003. 和为 K 的子数组 (Subarray Sum Equals K)

**难度**: 中等  
**标签**: 数组, 前缀和, 哈希表

---

## 📝 题目描述

给你一个整数数组 `nums` 和一个整数 `k`，请你统计并返回该数组中和为 `k` 的连续子数组的个数。

---

## 💡 示例

### 示例 1
```
输入: nums = [1,1,1], k = 2
输出: 2
```

### 示例 2
```
输入: nums = [1,2,3], k = 3
输出: 2
```

---

## 🎯 解题思路

**核心思想**: 前缀和 + 哈希表

**关键公式**: 
- 如果 `prefixSum[j] - prefixSum[i] = k`，则 `prefixSum[i] = prefixSum[j] - k`
- 即在位置 `j`，我们查找之前有多少个位置的前缀和等于 `prefixSum[j] - k`

**步骤**:
1. 使用哈希表记录每个前缀和出现的次数
2. 遍历数组，累加得到当前前缀和 `sum`
3. 查找 `sum - k` 在哈希表中出现的次数，累加到结果
4. 将当前前缀和加入哈希表

---

## 📊 复杂度分析

- **时间复杂度**: O(n) - 遍历一次数组
- **空间复杂度**: O(n) - 哈希表存储前缀和

---

## 🔑 关键点

- 初始化 `prefixSumCount[0] = 1`，处理从数组开头开始的子数组
- 先查询再更新，避免重复计算
- 适用于求连续子数组的和、个数等问题

---

## 📚 相关题目

- 最大子数组和
- 路径总和 III
- 连续的子数组和
